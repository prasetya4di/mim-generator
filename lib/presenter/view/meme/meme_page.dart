import 'package:flutter/material.dart';

class MemePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text("Pras nih bos"),
    );
  }
}
